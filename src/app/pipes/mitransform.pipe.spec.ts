import { MitransformPipe } from './mitransform.pipe';

describe('MitransformPipe', () => {
  it('create an instance', () => {
    const pipe = new MitransformPipe();
    expect(pipe).toBeTruthy();
  });
});
